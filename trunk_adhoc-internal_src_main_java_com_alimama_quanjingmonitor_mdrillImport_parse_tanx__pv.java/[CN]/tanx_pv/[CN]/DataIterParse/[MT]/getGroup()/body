{
  Date d=new Date((ts / 300) * 300000);
  return new String[]{String.valueOf(formatDay.format(d)),String.valueOf(formatMin.format(d)),String.valueOf(this.pid),String.valueOf(pvlog.getProductType())};
}
