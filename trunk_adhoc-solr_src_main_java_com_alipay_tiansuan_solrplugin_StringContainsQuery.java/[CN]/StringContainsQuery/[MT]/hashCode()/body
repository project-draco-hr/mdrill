{
  final int prime=31;
  int result=super.hashCode();
  result=prime * result + ((this.contains == null) ? 0 : Arrays.hashCode(this.contains));
  result=prime * result + ((field == null) ? 0 : field.hashCode());
  return result;
}
