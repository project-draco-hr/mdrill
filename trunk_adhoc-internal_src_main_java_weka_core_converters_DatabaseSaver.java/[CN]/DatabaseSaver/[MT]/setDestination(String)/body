{
  try {
    checkEnv();
    m_DataBaseConnection=newDatabaseConnection();
    setUrl(url);
    setUser(m_Username);
    setPassword(m_Password);
  }
 catch (  Exception ex) {
    printException(ex);
  }
}
