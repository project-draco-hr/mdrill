{
  if (first.freq > second.freq) {
    return 1;
  }
  if (first.freq < second.freq) {
    return -1;
  }
  if (first.score > second.score) {
    return 1;
  }
  if (first.score < second.score) {
    return -1;
  }
  return second.string.compareTo(first.string);
}
