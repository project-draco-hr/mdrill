{
synchronized (listlock) {
    search.reftime=System.currentTimeMillis();
    search.get().SolrCoreClearCache();
    clearlist.add(search);
  }
  try {
    clearPartion();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
