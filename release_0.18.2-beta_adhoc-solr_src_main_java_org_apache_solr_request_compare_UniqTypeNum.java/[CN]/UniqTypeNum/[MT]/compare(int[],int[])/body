{
  int len=a.length;
  int cmp=compare(len,a2.length);
  if (cmp != 0) {
    return cmp;
  }
  for (int i=0; i < len; i++) {
    cmp=compare(a[i],a2[i]);
    if (cmp != 0) {
      return cmp;
    }
  }
  return cmp;
}
