{
  IntArr doclist=this.join.getRight(doc,-1);
  if (doclist == null) {
    return false;
  }
  for (  Integer docr : doclist.list) {
    if (this.docset.exists(docr)) {
      return true;
    }
  }
  return false;
}
