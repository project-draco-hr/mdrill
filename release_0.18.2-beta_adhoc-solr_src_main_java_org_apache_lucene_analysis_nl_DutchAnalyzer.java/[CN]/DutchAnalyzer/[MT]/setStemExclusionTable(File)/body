{
  try {
    excltable=WordlistLoader.getWordSet(IOUtils.getDecodingReader(exclusionlist,IOUtils.CHARSET_UTF_8),matchVersion);
    setPreviousTokenStream(null);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
