{
  boolean allowadd=this.free.size() < 100;
  for (  BlockInterface<T> d : data.data) {
    if (d != null) {
      d.updateLasttime();
      if (allowadd) {
        this.free.add(d);
      }
    }
  }
}
