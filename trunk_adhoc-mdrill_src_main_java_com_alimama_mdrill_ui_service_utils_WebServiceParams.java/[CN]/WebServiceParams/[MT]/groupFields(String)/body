{
  ArrayList<String> groupFields=new ArrayList<String>();
  if (groupby != null) {
    String[] values=groupby.split(",");
    for (    String s : values) {
      if (s != null && s.length() > 0) {
        groupFields.add(s);
      }
    }
  }
  return groupFields;
}
