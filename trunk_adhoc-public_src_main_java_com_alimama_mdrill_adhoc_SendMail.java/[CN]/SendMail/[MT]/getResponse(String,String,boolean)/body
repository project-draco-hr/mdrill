{
  if (input == null || code == null || code.length() < 1) {
    return;
  }
  String line=input.readLine();
  if (line.startsWith(code)) {
  }
 else   if (shouldQuit) {
    System.out.println(line);
    throw new Exception(message);
  }
}
