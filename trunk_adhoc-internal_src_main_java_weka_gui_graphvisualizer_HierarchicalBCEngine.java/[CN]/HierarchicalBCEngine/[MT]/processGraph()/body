{
  origNodesSize=m_nodes.size();
  graphMatrix=new int[m_nodes.size()][m_nodes.size()];
  for (int i=0; i < m_edges.size(); i++)   graphMatrix[((GraphEdge)m_edges.elementAt(i)).src][((GraphEdge)m_edges.elementAt(i)).dest]=((GraphEdge)m_edges.elementAt(i)).type;
}
