{
  index--;
  index--;
  if (getShowStdDev()) {
    index--;
    if (index < 0)     return false;
    return (index % 3 == 2);
  }
 else {
    if (index < 0)     return false;
    return (index % 2 == 1);
  }
}
