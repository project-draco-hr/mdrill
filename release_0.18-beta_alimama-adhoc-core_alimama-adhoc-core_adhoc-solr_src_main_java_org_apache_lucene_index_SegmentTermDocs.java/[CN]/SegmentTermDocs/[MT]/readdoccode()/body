{
  if (isfrqCompress) {
    int count=0;
    while (true) {
      try {
        return ramread.readVInt();
      }
 catch (      IOException e) {
        this.resetRamRead();
      }
      count++;
      if (count > 10) {
        break;
      }
    }
    throw new IOException("readdoccode");
  }
 else {
    return ramread.readVInt();
  }
}
